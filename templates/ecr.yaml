AWSTemplateFormatVersion: '2010-09-09'
Description: >
  ECS CI/CD Lab — ECR nested stack. The application's container image repository.

Parameters:
  EnvironmentName:
    Type: String
  RepositoryName:
    Type: String

Resources:

  EcrRepository:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: !Ref RepositoryName
      ImageScanningConfiguration:
        ScanOnPush: true
      ImageTagMutability: MUTABLE
      EncryptionConfiguration:
        EncryptionType: AES256
      LifecyclePolicy:
        LifecyclePolicyText: |
          {
            "rules": [
              {
                "rulePriority": 1,
                "description": "Expire untagged (dangling) images after 1 day - every push to :latest orphans the previous image",
                "selection": {
                  "tagStatus": "untagged",
                  "countType": "sinceImagePushed",
                  "countUnit": "days",
                  "countNumber": 1
                },
                "action": { "type": "expire" }
              }
            ]
          }
      Tags:
      - Key: Name
        Value: !Sub ${EnvironmentName}-ecr
      - Key: Project
        Value: ecs-cicd-lab
      - Key: ManagedBy
        Value: cloudformation

Outputs:
  EcrRepositoryArn:
    Value: !GetAtt EcrRepository.Arn
  EcrRepositoryName:
    Value: !Ref EcrRepository
  EcrRepositoryUri:
    Value: !GetAtt EcrRepository.RepositoryUri
